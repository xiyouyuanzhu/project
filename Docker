# docker学习之路

## docker 为一种轻量化的容器，容器中装载了程序以及程序所需的环境以及配置信息，对于用户而言 ，配置好的容器可以很方便的进行部署以及批量化进行操作。
## 1.docker 与VM的区别
  （1）VM包括应用程序以及应用程序所需的库以及二进制文件和完整的OS，与容器相比，虚拟机完全虚拟化需要更多的资源。
  （2）容器包括应用程序及其依赖项，但是与其他容器共享OS内核，容器在操作空间中作为独立进程进行运行。
   (3) 由于容器相较与VM需要较少的资源，其不需要完整的OS，其易于部署且可快速启动。
